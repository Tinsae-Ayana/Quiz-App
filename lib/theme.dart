import 'package:flutter/material.dart';

class Mytheme {
  static ThemeData quizTheme = ThemeData(
      backgroundColor: Colors.white,
      primaryColor: const Color(0xff6649A9),
      errorColor: Colors.red,
      highlightColor: Colors.green);
}
